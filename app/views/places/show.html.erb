<div class="place_show_container">

    <%# event : prendre le même que sur la show de l'event %>
    <div class="row-show-place">
    <div class="col-12 col-lg-6">
    <div class="logo_team">
        <div class="png"><%= image_tag "Component 8 (2).png",height:60, width: 80%></div>
    </div>

    <%# description%>
    <div class="flexbox">
        <%# name %>
        <div class="name">
            <h1><%=@place.name%></h1>
        </div>
            
        <%# address %>
        <div class="address">
            <h1><%= @place.address%></h1>
        </div>
        
        <%# icone %>
        <%# phrase descriptif %>    
        <div class="sentence">
            <h1><%=@place.characteristic%></h1>
        </div>
    </div>
    <%# jauge %>

    <%# MAP %>
    <div class="map" style="width: 100%; height: 400px;"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
    <div class= "btn_homepage btn_show"><%= link_to "J'y vais", events_path %></div>
</div>

<%= render "shared/navbar"%>